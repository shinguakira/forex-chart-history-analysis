{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only permissions (tray, autostart)",
  "windows": [
    "main"
  ],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": [
    "autostart:default"
  ]
}
