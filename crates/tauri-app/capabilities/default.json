{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window (all platforms)",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "rspc:default"
  ]
}
